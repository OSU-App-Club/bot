import { helpCommand } from "./help";
import { joinGithubCommand } from "./join";

/**
 * Collection of all commands.
 * Want a new command? Add it here.
 */
export const commands = [helpCommand, joinGithubCommand];